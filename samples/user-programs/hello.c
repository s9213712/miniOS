// Host-side compilation sample for miniOS learning pipeline.
// This binary is intentionally built outside miniOS today.

#include <stdio.h>

int main(void) {
    puts("miniOS host C sample: hello from C");
    return 0;
}
