// Host-side C++ compilation sample for miniOS learning pipeline.
// This binary is intentionally built outside miniOS today.

#include <iostream>

int main() {
    std::cout << "miniOS host C++ sample: hello from C++" << std::endl;
    return 0;
}
